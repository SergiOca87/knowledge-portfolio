// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource postgresql {
  url      = env("DATABASE_URL")
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
  output   = "node_modules/.prisma/client"
}

model User {
  id                      String     @id @default(cuid())
  name                    String     @default("")
  email                   String     @unique @default("")
  publicEmail             Boolean    @default(true)
  password                String
  specialization          String     @default("")
  items                   Item[]     @relation("Item_author")
  sent                    Message[]  @relation("Message_sender")
  received                Message[]  @relation("Message_receiver")
  categories              Category[] @relation("Category_author")
  options                 Json?
  following               User[]     @relation("User_following")
  followers               User[]     @relation("User_followers")
  public                  Boolean    @default(true)
  linkedin                String     @default("")
  instagram               String     @default("")
  youtube                 String     @default("")
  website                 String     @default("")
  passwordResetToken      String?
  passwordResetIssuedAt   DateTime?
  passwordResetRedeemedAt DateTime?
  magicAuthToken          String?
  magicAuthIssuedAt       DateTime?
  magicAuthRedeemedAt     DateTime?
  from_User_following     User[]     @relation("User_following")
  from_User_followers     User[]     @relation("User_followers")
}

model Item {
  id                String     @id @default(cuid())
  title             String     @default("")
  mainImage         Json?
  status            String     @default("")
  date              String     @default("")
  visibility        String     @default("")
  description       String     @default("")
  singlePageContent Json?
  author            User?      @relation("Item_author", fields: [authorId], references: [id])
  authorId          String?    @map("author")
  categories        Category[] @relation("Category_items")
  urlTitle          String     @default("")
  url               String     @default("")

  @@index([authorId])
}

model Category {
  id       String  @id @default(cuid())
  name     String  @default("")
  items    Item[]  @relation("Category_items")
  author   User?   @relation("Category_author", fields: [authorId], references: [id])
  authorId String? @map("author")
  icon     String  @default("")

  @@index([authorId])
}

model Message {
  id         String  @id @default(cuid())
  text       String  @default("")
  sender     User?   @relation("Message_sender", fields: [senderId], references: [id])
  senderId   String? @map("sender")
  receiver   User?   @relation("Message_receiver", fields: [receiverId], references: [id])
  receiverId String? @map("receiver")

  @@index([senderId])
  @@index([receiverId])
}